/**
 * Host entry (package `main`): what the loader row `name: dsh-liangbiao`
 * inserted by cordis.patch.yml imports. The loader passes all module exports
 * as an object plugin.
 */
export { name, apply } from './host/index.ts'
