# 梁相

**众香成势，梁子显相。梁相还得梁人出！**

这是 [DeepSeek Harness](https://github.com/deepseek-ai/deepseek-harness) 的 WebUI 插件包，不是普通 npm 依赖。本页右侧自动生成的 `npm i dsh-liangxiang` **不会**把它装进 DSH。

源码与说明：[github.com/liang-today/dsh-liangxiang](https://github.com/liang-today/dsh-liangxiang) · 官网：[liang.today](https://liang.today/)

## 安装

先完全退出 WebUI：

```bash
export DSH_HOME="$HOME/.dsh"
npx --yes @deepseek-ai/dsh plugin --profile web add dsh-liangxiang
npx --yes @deepseek-ai/dsh web
```

已全局安装 `dsh` 时，把开头换成 `dsh`。页面边缘出现「今日梁相」即装好。升级重复同一条命令。也可以在设置 → 插件市场搜 `liangxiang`；也已列入 1024 Store 与 awesome-deepseek-harness。完整步骤见 https://liang.today/guide/#market 。DSH Desktop 必须先把 `DSH_HOME` 指到桌面自己的 harness 目录，见 https://liang.today/guide/#desktop 。

## 玩法

每天一道梁案。使用 DSH 烧掉的 Token 凝成香火，一炷夯或拉；社区香火汇成梁位，显出今日梁子。日终入祠。

独立社区项目，非 DeepSeek 官方产品。梁位不代表实名人数、真实民意或任何个人、机构立场。
