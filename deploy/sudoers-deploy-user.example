# Replace CHANGE_ME_DEPLOY_USER with the dedicated deploy account before install.
# SSH is key-only and direct root login is disabled; sudo remains auditable
# through the system journal.
Defaults:CHANGE_ME_DEPLOY_USER !requiretty
CHANGE_ME_DEPLOY_USER ALL=(ALL:ALL) NOPASSWD: ALL
